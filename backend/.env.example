DATABASE_URL="file:./dev.db"
PORT=3001
NODE_ENV=development

# Admin login (use strong values in production)
ADMIN_USERNAME=admin123
ADMIN_PASSWORD=skillmatch@2026

# Optional: bcrypt hash instead of plain ADMIN_PASSWORD (recommended for production)
# ADMIN_PASSWORD_HASH=$2a$10$...

# Required — use at least 32 random characters in production
JWT_SECRET=dev-only-change-me-use-openssl-rand-hex-32-in-prod
