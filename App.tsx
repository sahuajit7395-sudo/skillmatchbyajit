/**
 * Re-export so tools that expect `App.tsx` at the repo root can resolve it.
 * Run the app with: `npx expo start ./frontend` or `cd frontend && npx expo start`
 */
export { default } from './frontend/App';
